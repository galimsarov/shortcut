package com.example.a_core.e_abstractions.a_interface;

/**
 * Интерфейс — это «контракт»: что класс должен уметь, но не как он это делает
 */

interface Flyable {
    void fly(); // публичный абстрактный метод по умолчанию
}